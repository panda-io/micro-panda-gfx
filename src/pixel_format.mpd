// Physical strip/driver format — direct color only.
// Used by GfxDriver, RenderCtx, and Gfx strip buffers.
enum PixelFormat
    Mono   = 0   // 1-bit, no palette — OLED framebuffer (0=off, 1=on)
    RGB565 = 1   // 16-bit direct color

